packages:
  - "cli"
  - "cli/npm/*"

allowBuilds:
  # Native helpers `@vscode/vsce` uses for `vsce login`/`publish` (extension
  # signing, OS keychain credential storage). Not needed for `vsce package`.
  "@vscode/vsce-sign": false
  keytar: false

# Session Index

## 2026-08-29

- [2026-08-29-formatter-comment-placement.md](2026-08-29-formatter-comment-placement.md) — Started as a line-for-line port of `libm`'s `sinf.rs` to `examples/math/sinf.wx` (verified to **1 ULP** against `Math.sin` over ~500k inputs across every branch; `force_eval!` dropped since wasm has no FP status flags, `as f64` → `.promote()` since `as` is gated on `WasmScalar` equivalence). Running `wx fmt` on the result mangled the constants' hex comments, which turned up **three defects in `wx-fmt`**: trailing comments were moved down a line and reattached to the *next* item (`build_item_list` treated every gap comment as leading); comments were silently **deleted** in five places (`build_struct_declaration`, `build_enum_definition` and the match-arm builder only consulted comments when the body was *empty*, plus after the last top-level item and before the first item in a `mod` body); and blank lines carried the previous line's indent (`HardLine` wrote `'\n'` + indent, and a blank line is two of them). Researched how rustfmt and Prettier decide this — both **purely by source position, never by width** (Prettier's `attachComments` `ownLine`/`endOfLine`/`remaining` split; rustfmt's `comment_width` only wraps comment *text*, and only under `wrap_comments`, off by default). Adopted that rule: a comment trails iff nothing but horizontal space separates it from the code before it, `///` never trails, at most one comment per gap can trail (`//` runs to end of line). Explicitly **rejected the width-gated alternative** ("hoist if it fits") — it changes which item a comment appears to document and makes comments hop lines on unrelated edits. Whole rule lives in `split_trailing_comment`; everything routes through one `push_between(out, Before, After)` — the two enums naming what bounds the gap at each end (`Before::{Opener, Entry, SpacedEntry}`, `After::{Entry, End}`) — where an empty gap is just the line break, so no list special-cases "no comments". Key structural change: the line break moved from the end of iteration N to the start of N+1, so an entry can close its line with `;`/`,` first. `newline()` now trims trailing spaces, killing blank-line indent globally. `BlockComments` deleted (reduced to a one-line `block_has_comments`). Three API drafts rejected in review before the final shape. Also records a **process failure**: `git checkout --` on a file with pre-existing uncommitted changes destroyed the in-progress `Pattern::Struct` work, reconstructed from the already-modified `tests.rs`. Shipped green and then **regressed on `std/main.wx`** — a blank line between a doc comment and the `#[intrinsic]` below it, because **an item's attributes sit outside its span**, so routing every gap through one function meant now measuring across the `#[...]` line and reading its newline as a blank. `count_blank_lines` was misnamed (newlines minus one, only equal to blank lines when the region is pure whitespace) and became `has_blank_line`; the subtlety that cost a first attempt is that **neither bound is a line edge** — `from` is the end of an entry's *content*, with its `;`/`,` still following, so scanning the whitespace run from `from` stops on the separator and breaks three existing tests. Considered and rejected: giving `Attribute` a real span (compiler + parser change, snapshot churn, formatter-only benefit) and reconstructing the item start via a backward `#` search (~25 lines, heuristic, duplicated for `ImplItem`). Also **checked rustfmt rather than assuming** on whether a formatter should delete blank lines between a doc comment and its item — it preserves all four shapes and only collapses 2+ blanks to one; declined to normalize, keeping "the author's vertical whitespace is theirs" exception-free (contrast: in Go a blank line genuinely *detaches* a doc comment, so gofmt encodes a different model). Because author blank lines are preserved, the ones the broken binary had already burned into `std/main.wx` were indistinguishable from intentional ones and had to be deleted by hand; removing those newlines shifted every byte offset in the stdlib, so all 46 affected snapshots needed `cargo insta accept`. 925 `wx-compiler`, 56 `wx-lsp`, 32 → **43** `wx-fmt`.
- [2026-08-29-local-pattern-destructuring.md](2026-08-29-local-pattern-destructuring.md) — Finished `local <pattern> = expr` end to end; the AST/parser/formatter half (the `Pattern::Struct` work reconstructed above) was already done and the feature dead-ended at a single `_ =>` arm in `build_local_definition_statement` emitting "not yet supported". Stakes were higher than sugar: wx has tuple types, literals, layout and aliases but **no way to read an element** — there is no `t.0` (`ObjectAccess.member` is a `SymbolU32`, so a positional index can't even be represented), so destructuring is the only consumer of a tuple. Syntax chosen as `Point::{ x, y }` over Rust's bare form, with a full path — internal consistency with wx's `Point::{ x: 1 }` struct *literals*, and the path upgrade is what makes `geom::Point::{ x, y }` possible at all; parser mirrors `parse_path_expression`'s `::` walk rather than reusing `parse_path_segments`, which consumes the `::` then `break`s and so can't tell `Point {` from `Point::{`. Exhaustive with `..` (`Token::DotDot` already existed), but the parser deliberately **does not validate** `..` position or count — it records one `rest: Option<TextSpan>` and position carries no meaning. `t.0` deliberately **not** exposed: the TIR projection is compiler-internal, documented so nobody "finishes" it. Two corrections reshaped the implementation: `_` is a **drop, not a store** (`local _ = f();` reuses the existing `Assign { left: Placeholder }` → MIR `Drop` path and creates no local; a nested `_` emits nothing, projections being pure reads), and the scrutinee temp was put in a fused `DestructureDeclaration { value, bindings }` node so MIR spills it once — chosen over making `tir::Local.name` optional because **`mir::Local` has no name field at all**, making MIR already the layer where nameless temps live (it open-codes that same spill in seven places). That choice meant **`wx-lsp` needed zero changes** (destructured bindings are ordinary `tir::Local`s that `symbol_index` already walks, so go-to-def/rename/completion came free) and **no snapshots regenerated**. Recorded a rejected architectural idea worth keeping: an expression **arena would not remove the temp** — TIR is a tree whose evaluation order is structural, so two declarations sharing an `ExprId` would still emit the call twice; making sharing mean "evaluate once" requires memoization keyed on `ExprId`, i.e. value numbering, which is what `opt/` already is. In a tree IR the temp *is* the encoding of "evaluate once". Nesting is flattened into projection paths (`(x, (y, z))` → `[0]`, `[1,0]`, `[1,1]`) whose `PathStep` carries `aggregate_ty` explicitly so MIR never re-derives types or re-applies struct type args; `index` is the *declaration* index mapped through `decl_to_phys`, which matters because **tuples are alignment-sorted exactly like structs** — `(bool, i64, u32)` has `decl_to_phys == [2, 0, 1]` and a naive `value_index: index` silently returns the wrong element (covered by a test asserting the slots). Key finding: a **pre-existing panic** — `local t = (1, 2);`, a plain binding with no destructuring, passed TIR with zero diagnostics and crashed MIR at `lower_type_index`'s `unreachable!()`, because `is_comptime_number()` tests the type index itself and `(1, 2)` is a separately interned `Tuple { [INTEGER, INTEGER] }`; fixed with a `contains_comptime_number` traversal so it now correctly reports `E1002`. The traversal walks **tuples only**, narrowed from a first draft that mirrored `contains_infer` across every aggregate — confirmed empirically, not argued: there's no surface syntax for comptime types so anything the user wrote is concrete, `[1, 2]` already demands its own annotation, and `Box::{ v: 1 }` fails to coerce rather than inferring `T = INTEGER`. Needed **no new diagnostic codes** beyond the parser's `InvalidPattern` (E0015), reusing E1025/E1026/E1027/E1001 (the arm it replaced had no code at all, unlike the other 83 `report_*` helpers). Verified in emitted WASM, not just types: single `call 0`, `..` skipping fields, alignment-sorted params resolving correctly, `wx fmt` idempotent and the reformatted source rebuilding byte-identically. 899 → 925 `wx-compiler`, 56 `wx-lsp`, 43 `wx-fmt`. Left open: `report_unable_to_coerce` reads badly for an unresolved type param (`Box::{ v: 1 }` → "unable to coerce to type `T`", naming a parameter the user never wrote and describing coercion when it's an inference failure — 5 call sites, found while probing, deliberately not folded in); `local (a, a) = pair;` is accepted with shadowing where Rust rejects it (self-consistent with wx's same-scope shadowing, left as a deliberate difference); `Point::{ mut x }` doesn't parse.

## 2026-08-28

- [2026-08-28-crate-super-keywords-and-use-reexport-privacy.md](2026-08-28-crate-super-keywords-and-use-reexport-privacy.md) — Two features. **`crate`/`super` path-root keywords** (`self` explicitly out of scope): a test-case catalog for module resolution caught its own wrong assumption first (a sibling module isn't reachable by bare name — Rust needs an explicit root), motivating the feature. Design settled on pre-populating every namespace's own `.symbols` map at creation time with real `SymbolKind::Module` entries for `crate`/`super`, rejecting an initial special-case-every-path-walker draft — this means zero changes to any actual path-walking function, chaining (`super::super::x`) falls out for free, and a non-leading `super` is accepted (well-defined, cheaper to allow than forbid). Found and fixed three related panics (all via manually hovering `crate`): `namespace_name` indexing `dependency_names[&target]` unconditionally, unreachable for a package-root's self-reference; a `TypeFormatter` `Display` arm hitting the same bug independently; and `wx-lsp`'s `Hover`/`SignatureHelp` passing `root_package` instead of the hovered namespace's own package. `namespace_name` changed from returning a cached `SymbolU32` to `&str` directly, since every caller resolved it anyway and the no-real-name case (`crate` naming its own package root) can just answer with the literal text. Semantic-token exclusion for `crate`/`super` (they share `SymbolKind::Module` with real modules once resolved, so kind-dispatch can't distinguish them) landed as a hardcoded span-text check rather than new per-namespace tracking state; mirrored in VS Code's TextMate grammar and Zed's tree-sitter `highlights.scm` (no `grammar.js` change needed — traced why). **`use` re-export privacy**: picked over the other two catalog gaps (wildcard transitivity, wildcard-cycle detection) as self-contained and a real bug today, not a missing feature — every `use`, public or not, acted as `pub use`, since a re-export's `.symbols` entry is bit-for-bit the same `SymbolKind` a direct declaration would produce (needed so the same item reached through two re-export paths collapses instead of falsely conflicting), leaving only the *original* item's own `pub_span` to find. Design went through several rejected shapes — a side map on `ModuleNamespace`, a 3-state `Visibility` (challenged directly: "give an example where it's used" — none existed, collapsed to 2) — before landing on the one that reshaped everything downstream: `SymbolEntry::{Pending(DefId), Resolved{kind, visibility}}`, splitting `Pending` **out of `SymbolKind` entirely** so a still-unresolved claim has no `visibility` field to misread, rather than a silently-defaulted fake `Public`. That cascaded into a full mechanical migration (~40 `SymbolKind::Pending` sites), which DRY'd **9 duplicated** "do I still hold my own claim" checks into one `still_pending` helper, and unified what were briefly two near-identical insertion functions (`insert_symbol`/`insert_reexport_symbol`) into one once both just needed the caller to hand over a span. A real, unplanned bug rode along: `Module`'s own gating had been lumped in with genuinely-exempt kinds (`Memory`, import-block members — parser-rejects `pub` on both), but `pub mod foo;`'s span was always parsed and stored, just never *read*; a private `mod foo;` is now actually inaccessible from outside its parent+descendants for the first time, closing a CLAUDE.md-documented gap as a side effect. Both target tests pass for real, one needing its own wrong expected diagnostic fixed (`UndeclaredIdentifier` → the actually-correct `PrivateItem`, matching a direct private declaration). Manual testing (not planning) then found hover breaking on a rejected private reference, traced to `resolve_pending_namespace_symbol`'s early `Err(())` skipping every caller's own access-recording — checked against what the access list actually gates (`report_unused_items`, so the bug was compounding: private-and-rejected also got flagged dead code) before fixing, which surfaced a second, broader, pre-existing gap in the same function (no arm for `Function`/`Global` at all) and, on request, a third (`TraitAssocType`) — `_ => {}` removed entirely, now exhaustive over all 11 `SymbolKind` variants so the compiler forces a decision on the next new one instead of silently dropping it a third time (bitten once already, on `Module`). Renamed `record_type_kind_access` → `record_symbol_access` and, on request, moved from `Builder` to a private `impl TIR` method in `mod.rs`, matching existing precedent (`function_index`, `namespace_name`) for methods that touch only `self.tir.*` state. 896 → 899 `wx-compiler`, 56 `wx-lsp`, 32 `wx-fmt`. Left open: wildcard transitivity + cycle detection (coupled, deferred as one unit); glob-of-glob re-export privacy (`WildcardImport` has no visibility of its own yet — moot until transitivity exists, but won't need the `SymbolEntry` treatment when it does, since each glob already gets its own struct instance); method-call/struct-field privacy (pre-existing, unrelated)

## 2026-08-27

- [2026-08-27-error-absorption-and-poisoned-call-contexts.md](2026-08-27-error-absorption-and-poisoned-call-contexts.md) — Diagnostic-quality session, one theme throughout: a `TypeIndex::ERROR` operand marks a failure *already reported*, so downstream should absorb rather than re-report — but absorbing must not skip parts of the tree that still have something true to say. Started from `ptr.* = value` in `std/main.wx` reporting ``type `{unknown}` is not a pointer`` on a binding already diagnosed; `build_deref_expression` was sending `Type::Error` to its "not a pointer" catch-all. The fix that mattered wasn't silencing it but *what the new arm returns*: `Err(())` propagates, and `build_assignment_expr`'s `?` on `left` then bailed before its own match, so the RHS was never built at all — meaning both that errors inside it went unreported **and** that hover over it showed nothing, since `SymbolIndex` is built from `local.accesses` and an access is only pushed while an identifier expression is being built (A/B: `value accesses=1` vs `0`). `build_assignment_expr` already had an `ExprKind::Error` arm doing exactly the right thing; **it was simply unreachable**, so returning an error *expression* fixed the RHS with no new recovery code. That exposed a latent cascade next door — `build_compound_assignment_expr` had no such arm, so `p.* += x` reported a spurious `E1013` *and* still skipped the RHS; its arm collapses to `ExprKind::Error` rather than keeping the node shape because every compound variant carries a resolved `method_id: ast::DefId` and `DefId(u32)` has a private field and no sentinel (plain `Assign` keeps its shape only because it has no such field). Same shape again for calls: `build_call_expression`/`build_method_call_expression` `?`'d on callee resolution, so `self.reserve(Layout::of::<T>())` reported `E1049` and never looked inside the argument list — three of four call kinds affected, plain undeclared functions already working because an unresolved bare identifier yields an error-*typed* callee that lands in a pre-existing `_` arm whose comment already said it was checking arguments without parameter info. Checking arguments then surfaced a **pre-existing** (verified, not a regression) `E1002` cascade: a generic argument like `Layout::of::<T>()` has nothing to infer from, so it demanded an annotation for a gap the missing callee had opened. **Three designs rejected, two of them mine**: passing `expected_type: ERROR` alone does nothing (`infer_type_args` skips `ERROR` actuals, and even without that guard `Layout<M>` vs `Error` matches no structural arm); a `poisoned` flag on `ExprContext` (diagnostic policy shouldn't be threaded as state); and poisoning inside `infer_type_args` via a `(_, Type::Error)` arm — which works for the target case but **silently loses a real error**, since it binds `T = ERROR` from the *result seed before the argument loop*, so `same(1 as i32, true)` stops reporting the genuine `E1001` that `T = i32` from `a` would have caught. Initializing `type_args` to `ERROR` has the identical flaw plus needs edits at four allocation sites, three of which can't see the expected type. **What landed poisons *after* argument inference**, gated on `expected_result == ERROR`, and is net-subtractive: `had_unresolved` could only ever be true when `contains_infer(substituted_result)` was, so the flag, its second `if`, and a duplicate poison loop were all relaying one condition down a scope — collapsed into one block where the only difference between poisoned and unpoisoned is whether the diagnostic fires. **No helper added, twice deliberately**: the recovery loop in `build_call_expression` was a hand-rolled copy of the `_` arm below it, so a failed callee now becomes an error-typed expression and falls into that arm on its own, leaving the method path as the only site with its own loop. Performance measured rather than guessed — `infer_type_args` runs **145 times** per stdlib compile and the rejected poison arm fired 0 times across 663 existing tests. Also: 53 `interner.resolve(..).unwrap_or(..)` placeholder fallbacks converted to `.unwrap()` (matching 69 existing sites), two flavours of which were doing real damage — `unwrap_or("")` as a sort key in `symbol_index.rs:149`/`completion.rs`, where an empty fallback corrupts the ordering `partition_point` depends on and makes completions silently vanish, and as a match scrutinee at `builder.rs:4870`, where it reports "invalid memory limits attribute" and blames the user's source for an interner bug. Removed three std-math `codegen::` tests (math moving to a separately-installed library) — **three, not the two that were visible**, since the harness aborts the process on first failure; found by running all 108 in separate processes. Six regression tests added; notably every design above, baseline included, passed the existing suite unchanged, which is why they were needed. 879 `wx-compiler` (`codegen::` green for the first time), 55 `wx-lsp`, 32 `wx-fmt`. Left undone and documented: `SymbolKind` has no expression variant, so hovering a call/literal/cast/`a + b` shows nothing regardless of errors — needs a span→`TypeIndex` sidetable, deferred
- [2026-08-27-export-invariants-use-trees-and-wildcard-ambiguity.md](2026-08-27-export-invariants-use-trees-and-wildcard-ambiguity.md) — Parts 0–4 of `notes/export-resolution-and-use-trees-plan.md`, plus a delegated code-quality pass; only Part 5 (parse-then-reject export paths/turbofish) left. Started by checking the plan against the code rather than trusting it, which found six errors in it, all corrected in place — most consequentially that two cases it called "free" would have *panicked*: `get_symbol_location`'s `Pending` arm indexes `item_lookup[&def_id]` directly, so a `use` leaf that bound a name without a stub is a `Pending` with nothing behind it, and `use a::foo;` alongside `fn foo` would crash rather than report. That drove the central Part 2 decision — `use` leaves became **real items** (`tir.use_items` + `ItemIndex::Use`), one per named leaf with its own `DefId`, *restoring* the invariant "every `Pending` has a stub reachable through `item_lookup`" instead of adding an exception to it (rejected: a side map, which makes the invariant unenforceable; a distinct `SymbolKind::PendingUse`, which needs 29 sites audited of which only 5 are compiler-checked). **Part 1**: export-block invariants (`E1072`/`E1073`/`E1074`), whose substance was a data-model change — `tir.exports: HashMap` → `tir.export_block: Option<ExportBlock>` with the items *inside*, so one `Option` answers both "is there a block" and "what does it export" and they can't drift; two of the plan's three checks collapsed into one namespace comparison, and all three `return` *before* claiming the slot, since a misplaced block taking it would make the entry file's real block report as a duplicate of a rejected one and silently empty the ABI. **Part 2**: full nested `use` trees (`UseTree::{Glob,Name,Path,Group}`), `DefId` on the leaf not the item (three mechanisms key per-`DefId`, so `use math::{sin,cos};` needs two of each); provisional both-namespace claims that report *nothing* at prescan, because which namespace an import occupies isn't knowable until Phase 2 and `use math::add;` next to a local `struct add` is legal — the leaf that lost re-checks later, forcing any rival import first so the slot settles; and value-position `Pending` forcing, which never existed, making `const A: i32 = B; const B: i32 = 1;` a **pre-existing panic** that this fixed incidentally. **Part 3**: export reach through the scope chain — but verified by reverting the swap that it only fixes *globs*; Part 2 had already fixed named imports as a side effect, since a `use` leaf installs into the very map the old lookup read. **Part 4**: wildcard ambiguity (`E1075`, rustc's E0659 flattened into secondary labels), detected in the existing walk via `ScopeLookup::Ambiguous(Box<[(SymbolKind, SourceSpan)]>)` — the result **carries its own evidence** rather than a bool plus a second traversal duplicating the visibility rules; `SymbolKind` gained `PartialEq` so "same item through two globs" isn't a conflict (mutation-tested); ambiguity is per *scope level*, and a namespace's own symbols win outright, which is what makes the `help:` text true. The plan's reporting placement would have missed `foo()` — rustc's own example — because value identifiers resolve through `&self` code that can't push diagnostics. **Refactor pass** (subagent research, claims verified independently before acting): `resolve_symbol_forcing` was doing three scope-chain walks and three local-stack walks per identifier *and* carrying a hand-copied cycle guard — now delegates, and `resolve_symbol` deleted as dead; the two near-identical prefix walkers merged into one returning `PrefixWalk::{Resolved,Empty,NotAModule,Unresolved}`, which closed a hole where `use add;` was **silently accepted** because the old `Option` conflated "resolved"/"failed"/"no segments"; and `use` prefixes are now stored and walked once (`TIR::use_prefixes` with a three-state `PrefixTarget`, chosen over an `Option` so "unwalked" and "walked, goes nowhere" differ) — per-leaf walking had been pushing byte-identical `SourceSpan`s onto `accesses`, which wx-lsp turns into references, making **rename emit two overlapping edits at one range**. Measured and left alone: the ambiguity scan (~23 scope levels, ~9 glob edges per build). 809 → 846 `wx-compiler` tests, 31 `wx-fmt`, 52 `wx-lsp`. Left unfinished and documented: Part 5; `pub use` parsed but dropped (newly load-bearing now that leaves install real symbols); `use *;` still a silent no-op; glob prefixes necessarily silent on unresolvable prefixes; prescan glob resolution order-dependent within a file

## 2026-08-25

- [2026-08-25-manifest-required-cli-and-wx-lsp-wxjson-migration.md](2026-08-25-manifest-required-cli-and-wx-lsp-wxjson-migration.md) — Finished wiring the CLI to the `wx.json` manifest work designed in the 2026-08-23 sessions (previously deferred): `wx compile` renamed to `wx build` (builds whole projects, not one file), `wx build`/`wx check` always take a project directory, `wx format` redesigned to exactly one project per invocation (`--files` restricts to named files instead of walking the module tree, plus `--check`), `vfs::open_binary` (the old anonymous-file entry point) deleted outright, and all 19 `examples/*` given a `wx.json`. wx-lsp needed the identical migration — `discover_package_root` was still finding projects by looking for a file literally named `main.wx` — which surfaced five real, reproducible bugs fixed one at a time: a double-slash stdlib URI breaking go-to-definition into `std`; `build_symbol_index` skipping access-recording entirely for `Package`-kind (dependency) namespaces, breaking hover/goto-def/semantic-highlighting specifically for `std`/dependency tokens; a bare `todo!()` in qualified-bound resolution that could panic and permanently kill the LSP's single actor task (the actual explanation behind "everything stops working, then fixes itself" reports); a duplicate-access-recording regression introduced by fixing the previous bug, resolved by proving (via `symbol_kind_to_type`'s exactly-two call sites) that two ad-hoc inline access-recording pushes were now always redundant with the canonical recording point, and removing both; and a cache-invalidation bug where editing an open dependency's file could wipe an unrelated dependent's entire cache, because `owning_root` reverse-scanned a genuinely many-to-many map (`published_by_root`) as if it were one-to-one — fixed with a real 1:1 `own_root: HashMap<PathBuf, PathBuf>`. Also redesigned the LSP's debug logging into a `Trace` struct after several rejected iterations (flat lines with embedded paths, then a tree-branched block, then a rejected `info`/`steps` split, then a rejected fully-generic `Vec<(&str, String)>`) landing on `Trace { path: PathBuf, steps: Vec<(&'static str, Duration)> }` — one required typed fact (the file that triggered the trace) plus timed steps, rendered as an indented tree block so it doesn't get lost in VS Code's Output panel (which only timestamp-prefixes a message's first line). Found and fixed a stray, unrelated manifest-schema drift along the way (root `wx.json`'s `format` section had been renamed to `fmt` with no matching `#[serde(rename)]`, so it was silently doing nothing); found but explicitly left unfixed: pre-existing bugs in the `factorial`/`globals`/`raycaster`/`vec` examples, surfaced only by giving them manifests. Package-identity-based dependency caching/dedup (to avoid double-compiling a shared dependency like `pow`) explicitly deferred as too large for now.
- [2026-08-25-module-collision-diagnostics-and-phase1-redesign.md](2026-08-25-module-collision-diagnostics-and-phase1-redesign.md) — Follow-up to the same day's namespace refactor, closing its first follow-up item (`ensure_module`'s duplicate rule). Three name-owning mechanisms — a `wx.json` dependency, an `import { }` block, and a user `module` — could each silently steal another's binding with zero diagnostic; worst case, a file-declared `module std;` didn't just merge but *replaced* the real `std` binding outright, so `use std::*;` silently resolved against the user's own file and surfaced as a cascade of unrelated "undeclared type" errors with nothing pointing at the cause. All three now report `E1000` (`DuplicateDefinition`). Fixing it properly (not just adding a kind-check) meant splitting `TIR::build`'s Phase 1 into two passes: 1a creates every file's namespace directly from vfs's `SourceModule.declaration` — safe because `ModuleId`s are assigned in push order, so a parent's is always numerically less than its children's — with every `ModuleDecl` field set exactly once at construction; 1b (`pre_scan_item`) is now a pure reader of `file_namespaces`, and its `ModuleDeclaration` arm (previously a call whose return value was discarded, existing solely for a `pub_span` side effect) is a no-op. This eliminated a pre-existing `if decl.pub_span.is_none() { decl.pub_span = pub_span }` mutation entirely, by removing the two-callers-racing situation it was defending against rather than just guarding it. Along the way, vfs's `SourceModule` gained a proper `ModuleDeclaration { parent, name, pub_span }` bundling what were three independently-optional fields that always existed or didn't *together*. New shared `check_module_collision` helper (direct-scope only, not the ancestor-walking `lookup_global_symbol`) is used by `ensure_module` (now down to one caller: inline `module foo { }` blocks), Phase 1a itself, and `pre_scan_item`'s pre-existing, unrelated `Import` arm (which had the identical unconditional-`insert_symbol` bug — fixed with the added nuance that the missing-alias fallback identifier, an unescaped import-path string like `"wasi_snapshot_preview1"`, is never a real user-chosen name and so is excluded from both the check and the binding entirely, not just the check). Separately: vfs's child-declaration scan was flat, so `module extra;` nested inside an inline `module utils { }` block was invisible rather than diagnosed — fixed as detection-only (new `E2006`), deliberately not resolution: Rust supports this (accumulating a directory segment per inline nesting level) but it was rejected as adding a real conflict with the Phase 1a/1b split (a nested file declaration's parent *namespace* wouldn't exist until Phase 1b creates it) on top of the readability cost of not being able to tell where a `module foo;` file lives without walking up through its enclosing inline wrappers. **Also fixed**: a module's children used to resolve relative to its own file's *physical* directory (`self.modules[parent_module_id].file_path.parent()`) rather than a directory named after the module itself, so a plain-sibling file like `a.wx` (living directly in `src/`, not `src/a/mod.wx`) never actually nested `shared` under `a` when it declared `module shared;` — it landed in `src/shared.wx`, a plain sibling of `main.wx` itself, indistinguishable from (and colliding with) any other sibling file declaring the same name. Rust's own diagnostic (`E0583`, suggesting `.../a/shared.rs`) confirms it always grants a module its own directory per path segment regardless of file form; fixed by threading an accumulated `owned_dir: AbsolutePath` through `Loader::load_module`/`resolve_child_module_path` (`<parent's owned_dir>/<child's name>`, computed the same way regardless of which form resolved the child's own file) instead of deriving it from the file path, making `math.wx` and `math/mod.wx` fully interchangeable for what directory `math`'s children live in — and making the collision impossible by construction rather than needing a new restriction or diagnostic (an unresolvable path is just an ordinary `ModuleFileNotFound`). 828 `wx-compiler` + 53 `wx-lsp` passing (six new regression tests, no regressions)
- [2026-08-25-package-namespaces-and-typeformatter-analysis.md](2026-08-25-package-namespaces-and-typeformatter-analysis.md) — Implemented Stages A1 and B of the previous session's design. **A1**: `PackageGraph.name` deleted, `PackageKind`/`PackageManifestKind` now fieldless — a package has no name of its own, it's known by the key a dependent declared it under, so the name lives on the `(declaring package, key)` edge as `dependencies: HashMap<SymbolU32, PackageId>` plus its inverse `dependency_names`, well-defined only because `add_dependency` keeps the mapping injective and reports its own diagnostics (new `E2004` `StdPackageAsDependency`, `E2005` `PackageDeclaredTwice`); the manifest key is settled as a *declaration*, not an alias, which keeps a future `use x as y` orthogonal instead of conflicting; `package_by_name` deleted in favour of a non-optional `stdlib_package: PackageId`. **B**: every package — root included — now owns a root namespace, which un-overloads `None` (it had meant both "the root package's own top-level scope" and "no parent") and fixes three bugs at once, confirmed empirically beforehand via a root package calling `shared::from_shared()` with zero diagnostics despite never declaring `shared`: transitive dependencies nameable from anywhere, `std` able to see the root's private items, and `module std;` silently merging into the stdlib's namespace. Net-subtractive — `TIR.root_symbols`/`root_wildcard_imports`, `lookup_global_symbol`'s global fallback, `ModuleNamespace.name`, the `PackageId` in `ModuleDeclarationKind::Package`, and the `is_ancestor_or_self` crate-root guard (a workaround for exactly this conflation, documented as a pitfall in CLAUDE.md) are all gone; replaced by `package_namespaces` + `file_namespaces` (a `Vec` indexed by `FileId`) and a `package: PackageId` on every namespace. Dependency edges are materialized as ordinary `Module` symbols in the declaring package's own namespace — a dependency *is* an implicit `module <key>;` at the top of the entry file — so nothing global is involved and a package's dependencies stay invisible to its own dependents. Naming a namespace became contextual and total (`namespace_name(ns, packages, from) -> SymbolU32`) rather than `Option`, since dropping names from diagnostics is worse than threading context; storing the name on `Type::Namespace` was rejected (a name is not a property of a type). `Option<NamespaceIndex>` removed codebase-wide (~40 signatures). **`TypeFormatter` analysis**: `type_params` was **dead** — written by `with_type_params`, never read, since the `Type::TypeParam` arm resolves names entirely from `owner` + `tir`; removed with its three no-op LSP call sites. The planned "collapse `interner`/`id_generator`/`files`/`packages` into one `&mut CompilationUnit`" was **proven impossible** and its TODO (which had claimed feasibility by checking the wrong lifetime) corrected in place: `ast_nodes` holds `&'ast ast::Item` pointing *into* the same `CompilationUnit`, legal only because `packages` and `interner` are disjoint fields — behind one `&mut` the AST refs must reborrow out of it, poisoning the builder for every later `&mut self` phase (E0502, reproduced standalone). Consequence: the formatter needs shared `interner` + shared `packages` while the builder holds mutable `interner`, so no single owner exists to name; the achievable form is a *narrower* `NameContext { interner, packages, from }`, deferred. Also found: `packages`/`from` serve exactly one match arm (`Type::Namespace`) for all 89 call sites; `display_type`/`display_bounds` should be infallible (30 `.unwrap()`s, 6 `.unwrap_or_default()`s that render an empty type name into hover text); `pub interner` exists only for four sites, two of them `.unwrap_or("?")`

## 2026-08-23

- [2026-08-23-package-manifests-path-types-and-format-cli-design.md](2026-08-23-package-manifests-path-types-and-format-cli-design.md) — Implemented Steps 0–3 of the `wx.json` package-manifest plan (`Crate*`→`Package*` rename, `PackageKind`, `PackageManifest` parsing, dependency resolution with implicit-`std`/diamond-dedup/cycle-detection via `open_package`); Step 4 (wiring the CLI to actually use it, the plan's original motivating bug) explicitly deferred. Grew into a full `AbsolutePath`/`RelativePath` path-representation redesign across every frontend, which surfaced a real `wx-lsp` regression (fixed via a new `FileOrigin{Local,Virtual}` on `vfs::File`, replacing a "starts with `/`" heuristic that broke once stdlib paths also became `/`-rooted) and a real diagnostics-duplication bug (`ast.diagnostics` was being cloned into `PackageGraph.diagnostics`, the field renamed to `linker_diagnostics` and the clone removed, with four consumers — `wx-cli`, `wx-lsp`, `wx-compiler-wasm` — updated to explicitly chain both sources). Ended mid-design on `wx format`'s new CLI surface (whole-package + explicit-file-list + `--manifest` config sourcing) — three concrete shapes discussed, none decided, explicitly punted to next session. Also found: a stray, unregistered root `wx.json` sketching a future schema (`meta`/`format` sections) that goes beyond what this plan's design doc scoped in; three pre-existing, unrelated failing tests (`sin`/`cos`/`scale_pow2` not yet implemented in `std/main.wx`)

- [2026-08-23-format-cli-and-stdlib-provider-design.md](2026-08-23-format-cli-and-stdlib-provider-design.md) — Design-only, resolving the previous session's Open question 1. **`wx format`**: none of the three shapes (A/B/C) from that devlog was picked; the problem was re-decomposed into two orthogonal axes after noticing `load_package` already walks the module tree from an entry file with no manifest involved — so "format a package" and "format a module tree without a manifest" are the *same call*, and the manifest is purely a config source. Selection: directory arg → `<dir>/main.wx` + module tree, `.wx` arg → that file only (shallow, against rustfmt's follow-children precedent — formatting is a write, and `wx format $(git diff --name-only)` must mean exactly those files), default arg `.`, non-package directory is an error not a skip; filesystem-walk-for-directories rejected because it drags in an ignore-rules subsystem wx has no concept of. Config: `--manifest` → directory-target's own `wx.json` → `./wx.json` → defaults, deliberately no ancestor walk, with `resolve_config(file) -> RendererConfig` keeping the nearest-ancestor upgrade a surface-compatible (not behavior-compatible) internal change, and `wx-lsp` sharing the same call so editor and CLI can't disagree. Monorepos are a workspace problem, deferred — shell globs cover it today because per-*target* config resolution is what multi-package formatting actually needs. **Package `name`/stdlib provider**: audit found the manifest `name` reaches exactly one consumer (`package_kind`'s `name_override.unwrap_or(name)`, which only fires for the root) and `package_by_name` has exactly one non-test consumer (`resolve.rs:95`), so `name` comes off `lib` manifests entirely — names belong to edges (dependency keys), registry identity is a separate future `meta.name`, and self-qualification should eventually be a `package::` keyword (cheap: `package_namespaces` already exists), spelled `package` not `crate` since the `Crate*`→`Package*` rename just landed. Implicit std becomes a merged default (`dependencies.entry("std").or_insert(Builtin)` on the root only), deleting `resolve.rs:94-97`'s `package_by_name` probe and collapsing default/override into one path; opting out is a third fieldless package type `{"type": "std"}` (chosen over a `DependencySource::None` variant, a `no_std` boolean whose 2×2 has an undefined cell, a CLI flag, opt-in std, and deriving it from tag definitions — all recorded with reasons). `CompilationUnit.stdlib_package` is a non-optional `PackageId` enforced via `build(root_package, stdlib_package)`; all 12 call sites already call `load_stdlib()` and discard the id it returns. Two things this surfaces: `#[tag = "..."]` is completely ungated today (`builder.rs:4378-4384`, bare `tagged_items.insert`, last-wins across packages — any user package can silently hijack `+`), and `resolve_operator_trait`'s `panic!`s become reachable from a two-line `wx.json` the moment any user-facing opt-out exists, so panic→diagnostic is a prerequisite. Also specified: a 5-line `cargo test` that loads the *embedded* stdlib as the root package, complementary to `wx check` on `std/`

## 2026-08-18

- [2026-08-18-bitwise-operators-enum-anchor-generic-trait-bugs.md](2026-08-18-bitwise-operators-enum-anchor-generic-trait-bugs.md) — Bitwise operator-overload traits (`BitAnd`/`BitOr`/`BitXor`/`Shl`/`Shr`/`BitNot`), same shape as the existing arithmetic traits; comparison operators explicitly deferred. Along the way: widened integer literals from `i64` to `u64` (fixed `i8::MIN`-class boundary bugs and `u64::MAX`/`i64::MIN` literals that couldn't be written at all, plus `eval_const_expr`'s Div/Rem signedness); enum variants now require an explicit anchor value before auto-increment is legal (new `E1071`, closes a real `Ordering { Less, Equal = 0, ... }` silent-collision bug); generic-bound dispatch for unary operators (`Neg`/`BitNot`) implemented, closing a gap `build_operator_dispatch` (binary) already didn't have. Two real bugs found via a new `Hasher`/`Hash` example (`examples/hashing/main.wx`): a trait-impl method with its own type params (independent of the impl block's) lost them entirely in TIR (`AstNodeRef::TraitImplFunction` hardcoded `type_params: Box::new([])`), and, once fixed, the identical shape crashed codegen via a MIR abstract-dispatch bug conflating "impl block is generic" with "impl's copy of the method is generic". Also swept up several genuinely-dead code paths (a provably-unreachable coercion arm, five fully-unused `ast::BinaryOp` predicate methods, a 130-line-to-30-line coercion collapse). Found but deliberately **not implemented**: memory-tag positions (`Mem::&T`) never check that `Mem` is actually bounded by `Memory` — design settled (resolve `Memory`'s `TraitIndex` once after Phase 1 pre-scan, like the pre-interned `TypeIndex` table, not per-call) but left for a future session

## 2026-08-17

- [2026-08-17-compound-assignment-trait-dispatch-design.md](2026-08-17-compound-assignment-trait-dispatch-design.md) — Design-only: `+=`/etc. as sugar over resolved `Add`-style trait dispatch. Records the rejected shapes and why (full TIR-level `place = place.add(rhs)` desugar blocked by `Expression`/`Place` not being `Clone`, plus a confirmed pre-existing double-evaluation bug in `lower_compound_assignment`'s `Load{place}` handling independent of trait dispatch; a single `CompoundAssign` node with `Option<DefId>` conflated "typeset-bounded" with "genuinely unsupported"; a target-kind split into `LocalSet`/`GlobalSet`/`FieldSet` left `FieldSet` depending on an invariant enforced elsewhere). Key insight: typeset-bounded (`Mem::Size`) and generic-not-yet-concrete (`T: Add`) are the same category — both resolve via monomorphization's existing substitution machinery, confirmed against how `GenericMethodCall` already resolves abstract trait methods at MIR-lowering time via `find_trait_impl`. Final design: new TIR-only `BinaryOp` excluding assignment operators, a new `Assign` node for plain `=`, and four compound-assignment nodes split two ways (resolved-now vs. deferred-to-monomorphization × plain-`Expression`-target vs. `Place`-target) — `CompoundAssign`/`GenericCompoundAssign`/`CompoundStore`/`GenericCompoundStore`
- [2026-08-17-compound-assignment-implementation-and-deferred-bugs.md](2026-08-17-compound-assignment-implementation-and-deferred-bugs.md) — Implemented the design above end to end (609/104 → 711/2 passing). Along the way: fixed `report_unused_items` firing on trait impl methods (reused the already-free `TypeParamOwner::TraitImpl(_)` signal, matching Rust); fixed `opt::tests::TestCase::get_first_func`'s stale "no stdlib" assumption via tag-based lookup, which led to the real bug — `extend_bindings`/`extend_bindings_in_place` unconditionally seeding default-value nodes for inlined-call scopes whose locals were already populated via `LocalSet`, fixed and independently adversarial-reviewed. That review surfaced two items, both since resolved: a chained-inlining flat-offset collision that silently clobbers a staged value with a sibling's (confirmed value-correctness bug via wasmtime, same root cause as `test_lerp`'s trap — fixed later the same day, see [2026-08-17-chained-inlining-offset-collision-fix.md](2026-08-17-chained-inlining-offset-collision-fix.md)), and `char` missing `impl Sub`/`impl Add` — resolved, not a gap: deliberately no impl at all, since unconstrained arithmetic could produce a `char` bit pattern outside the valid Unicode-scalar-value range with no checkpoint to reject it; arithmetic requires an explicit cast to an integer type and back, matching `std/main.wx`'s own `to_ascii_uppercase`/`to_ascii_lowercase` idiom
- [2026-08-17-chained-inlining-offset-collision-investigation.md](2026-08-17-chained-inlining-offset-collision-investigation.md) — Deep dive on the chained-inlining bug above: exact mechanism (`LocalSet` writes directly into a shared, mutating vector, unlike `Block`'s own clone-extend-discard, which is what makes `if`/`else`/`Aggregate`/etc. safe). Two fix candidates worked out: (1) stop `compute_locals_offsets` from sharing offsets between siblings at all — trivial, provably safe, zero cost since the vector it feeds is discarded before codegen, but felt like patching around the real problem rather than fixing it; (2) make `inline_call`'s own scope tree honestly reflect non-exclusive nesting. Within (2), a first attempt (thread scope through `Call`'s own arguments) is proven *insufficient* via a hand-traced counterexample (`test_lerp`'s `a + (b-a)*t` — three separate inlining sweeps, one per operator method, where an already-substituted `Block` from an earlier sweep still collides); a second attempt (chain onto whichever of the true call site or the globally-last-created scope has the larger occupied range) is hand-verified correct against both known failure shapes and needs no explicit threading at all. Neither implemented that day — user asked to stop and just record findings; everything reverted, back to pre-investigation baseline (715/2). Superseded by an even simpler fix, see below
- [2026-08-17-chained-inlining-offset-collision-fix.md](2026-08-17-chained-inlining-offset-collision-fix.md) — Fixed, later the same day, with a *different, simpler* design than either candidate above: instead of protecting a new per-call scope from colliding with a sibling's, stopped creating that scope at all — `inline_call` now appends the callee's own root-scope locals (its parameters, and any `local` it declares directly) onto the caller's *existing* scope at the call site, so a second inlined call there just gets index ranges past the first's, nothing to compare or protect (`Vec::push` can't collide with itself). The break-target wrapper scope still exists unconditionally but never holds locals, so it's always safe to parent at the call site directly. Generalized the pre-existing `Rebaser` (used elsewhere for combining globals' initializers into the start function) to cover both the new root-redirect case and the old plain-shift case, removing a duplicate tree-walk. One accepted regression: `opt::tests::test_simple_add` now produces one extra dead SSA node per inlined call at a function's root (root-scope default-seeding fires for locals inlining adds past the declared params) — harmless (reused via CSE, doesn't affect WASM output), test updated rather than fixed. Also checked and ruled out (with a new empirical regression test, not just on paper) whether `MIR::build_start_function`'s globals-combining shares the same bug shape — it doesn't, because each global's init is always a self-contained `Block` value routed through the same clone-then-discard mechanism that makes `if`/`else` safe. 732 passed / 0 failed

## 2026-08-16

- [2026-08-16-pointer-reference-split-design.md](2026-08-16-pointer-reference-split-design.md) — Design-only: replace pointer/slice/array `mutable: bool` with exclusive/shared kind (`*T`/`&T`, `*[T]`/`&[T]`, `*[T; N]`/`&[T; N]`), no move-checking/`Drop`/lifetimes; AST/TIR stay flat so `[T]`/`[T; N]` can never appear bare; postfix `.&`/`.*` kept, `.&mut` dropped entirely and `.&` never produces `*T` (rejected a context-elaborated exclusive `.&` as reintroducing Rust's reborrow problem); the one real call site needing it (`wasi_preview1_port`'s union simulation) solved via a null-pointer `offsetof` idiom using only existing operators, no new intrinsic; surfaced and deferred a much bigger feature, a WIT-style `variant` item (payload-carrying tagged union, compiler-assigned tags) plus real `match` destructuring, as the actual long-term fix
- [2026-08-16-primitives-as-items-and-use-scoping.md](2026-08-16-primitives-as-items-and-use-scoping.md) — Primitives turned into real `#[intrinsic] pub type X;` items (DefId, LSP go-to-def/hover) instead of a hardcoded string match; unrelated `wx-fmt` bug found (`impl` block `pub const` silently lost `pub`, broke ~100 tests); investigating "std auto-imported everywhere" surfaced that a root-level `use X::*;` is currently crate-wide in wx, not per-file like Rust — `lookup_global_symbol`'s ancestor-walk conflates privacy-ancestry with import-lookup, confirmed empirically via a multi-file probe; also found duplicate inherent-impl members (e.g. two `impl f32 { const PI }`) are only ever caught at first use, or not at all if unused — both noted as their own future tasks, not fixed today
- [2026-08-16-generic-typeparam-arithmetic-gap.md](2026-08-16-generic-typeparam-arithmetic-gap.md) — Found while adding `ptr::align_up`: two gaps in operator support for typeset-bounded types. Deferred: bare type params bounded directly by a typeset (`<Size: PointerSize>`) don't support operators at all (only concrete primitives and `AssocTypeProjection`s like `Mem::Size` do) — `align_up` shipped bounded by `Mem: Memory` over `Mem::Size` instead, matching the rest of the `ptr` module. Fixed: unary `^`/`-` on an `AssocTypeProjection` crashed the compiler outright (raw `panic!`, not a diagnostic) and binary bitwise ops on the same shape failed to type-check — both fixed by extracting a shared `is_typeset_bounded_assoc_type` primitive and composing it at all four operator-checking call sites, replacing the panic with a real diagnostic
- [2026-08-16-inline-intrinsic-call-graph-bug.md](2026-08-16-inline-intrinsic-call-graph-bug.md) — Pre-existing crash (confirmed via `git stash` against unmodified `develop`, using only `f32_sqrt`): an `#[inline]` function calling an `#[intrinsic]` function panics `mir/inlining.rs`'s Kahn-queue pass, because resolving a bare function-value reference (`tir::ExprKind::Function { id }`, on the path to every call) unconditionally recorded a call-graph edge with no intrinsic check, unlike the other three call-dispatch arms — fixed with a new `is_intrinsic` guard, plus a second independent gap where ordinary (non-method, non-generic) calls never recorded an edge at all. Surfaced by the new operator-overloading primitive impls (`impl Add for i32`-style, all `#[inline]`, all calling a raw intrinsic). Considered and rejected making intrinsics real `#[inline]` `mir::Function`s instead (no performance win — eager substitution is already stronger than `#[inline]`; would need new monomorphization machinery for generic intrinsics)

## 2026-08-11

- [2026-08-11-ownership-borrowing-allocator-design.md](2026-08-11-ownership-borrowing-allocator-design.md) — Design-only discussion: owned/borrowed (no `&mut`) ownership model reusing existing `*T`/`*mut T`/`[]T`/`[N]T` syntax, scope-based lifetime inference deferred `'a` syntax, manual-vs-automatic cleanup unified via a minimal opt-in `Drop` trait, and an open allocator-identity problem (memory tag ≠ allocator; bump/arena allocators need arena-scoped escape checks instead of per-object `Drop`)
- [2026-08-11-ownership-implementation-strategy.md](2026-08-11-ownership-implementation-strategy.md) — Follow-up: checking belongs at TIR (not MIR) since control flow, `Place`/`PlaceKind`, per-binding use-lists, and scope-parent chains already exist there pre-monomorphization; move-vs-Copy resolved as a transitive-affine rule (`*T` itself, or transitively via inline fields, or explicit `impl Drop`) with `&T` as Copy but still scope-checked per copy

## 2026-07-21

- [2026-07-21-match-expression-and-br-table.md](2026-07-21-match-expression-and-br-table.md) — `match` expressions added end-to-end (AST reuses `->`, TIR patterns + exhaustiveness, MIR `Switch` kept as a genuine N-way node, Opt decides `br_table` vs. `if`/`else` chain once via `should_use_br_table`, Scheduler `emit_switch_br_table`, Codegen `BrTable` opcode); two `Block::parent` depth-chain off-by-one bugs found and fixed via wasmtime execution testing; unrelated pre-existing `break`/`continue` loop-param commit bug found and fixed along the way

## 2026-06-24

- [2026-06-24-vscode-extension-setup.md](2026-06-24-vscode-extension-setup.md) — CI publish workflow (cross-platform `.vsix`), `ExtensionMode`-based binary resolution, actionable error messages, config-change restart, FileSystemWatcher leak fix, tmLanguage keyword scope split, pointer/deref highlighting

## 2026-06-21

- [2026-06-21-assoc-type-chains-comptime-inference.md](2026-06-21-assoc-type-chains-comptime-inference.md) — Nested `AssocTypeProjection` chains (`A::M::Size`), `are_scalar_compatible` for abstract-memory pointer casts, `null_mut` in std, formatter `::` before intrinsic type args, cross-branch comptime inference in if-else

## 2026-06-19

- [2026-06-19-type-resolution-cleanup.md](2026-06-19-type-resolution-cleanup.md) — `Result<TypeIndex, ()>` return for type-resolution helpers, `register_module_access` in type-position paths, test audit: weak assertions hardened to `has_error_code`, false-positive test removed
- [2026-06-19-inline-generics-vec-fmt.md](2026-06-19-inline-generics-vec-fmt.md) — `#[inline]` propagation to mono instances, removed memory method special case, receiver into `arguments[0]`, removed `SIZE`/`ALIGN` auto-constants, generic bump allocator with `@size_of`/`@align_of`, generic `Vec<M, T>` example, formatter generic param support for structs and impl blocks
- [2026-06-19-null-pointers-mut-coercion-sized-design.md](2026-06-19-null-pointers-mut-coercion-sized-design.md) — `null()` intrinsic, `*mut T → *T` implicit coercion, mixed-mutability pointer comparison, bump allocator linked list rewrite, `Sized` trait and typeset-bounded const design discussion

## 2026-06-18

- [2026-06-18-narrow-loads-slice-intrinsics-purity-design.md](2026-06-18-narrow-loads-slice-intrinsics-purity-design.md) — Narrow load/store bug fix (`MemAccess` enum), `@slice_from_parts` intrinsic, purity inference pass design (SCC-based, integrated into inlining pass)

## 2026-06-16

- [2026-06-16-slice-range-and-generic-impl.md](2026-06-16-slice-range-and-generic-impl.md) — Generic `impl<M, T> M::[]T` blocks, exclusive slice range `arr[i..n]`, lexer float/`..` bug fix, `from > to` trap
- [2026-06-16-type-formatter-and-unit-cleanup.md](2026-06-16-type-formatter-and-unit-cleanup.md) — `TypeFormatter` context-aware generic display, intrinsic LSP fix, plan for `unit` → `()` unification and `Grouping` removal
- [2026-06-16-abstract-memory-trait.md](2026-06-16-abstract-memory-trait.md) — Removed Memory32/Memory64 sugar traits, `impl Trait` syntax removal, `TypeParamOwner::Trait` for trait const Self, generic array indexing fix via `AssocTypeProjection`
- [2026-06-16-lang-items-and-codegen-fixes.md](2026-06-16-lang-items-and-codegen-fixes.md) — Lang items map (`#[lang = "key"]`), MIR Slice lowering, codegen test suite fixes, `DefId` architecture discussion

## 2026-06-15

- [2026-06-15-tir-ast-refactor.md](2026-06-15-tir-ast-refactor.md) — Refactored TIR builder AST registry: Vec+HashMap replacing two HashMaps, named structs, `file_id`/`namespace` moved to `AstEntry`
